# =====================================================================================================
# ――――― Слой 1 ―― Super + {} ―― Быстрый доступ ――――――――――――――――――――――――――――――
# =====================================================================================================
bindr   = Super, Super_L,               exec, $HotKey panels launcher false                 # Открыть панель приложений
bind    = Super, Space,                 exec, $HotKey panels dashboard false                # Открыть панель дашборда
bind    = Super, grave,                 workspace, m-1                                      # Предыдущий рабочий стол
bind    = Super, 1,                     workspace, 1                                        # Рабочий стол №1
bind    = Super, 2,                     workspace, 2                                        # Рабочий стол №2
bind    = Super, 3,                     workspace, 3                                        # Рабочий стол №3
bind    = Super, 4,                     workspace, 4                                        # Рабочий стол №4
bind    = Super, 5,                     workspace, 5                                        # Рабочий стол №5
bind    = Super, 6,                     workspace, 6                                        # Рабочий стол №6
bind    = Super, 7,                     workspace, 7                                        # Рабочий стол №7
bind    = Super, 8,                     workspace, 8                                        # Рабочий стол №8
bind    = Super, 9,                     workspace, 9                                        # Рабочий стол №9
bind    = Super, 0,                     workspace, 10                                       # Рабочий стол №10
bind    = Super, minus,                 workspace, 11                                       # Рабочий стол №11
bind    = Super, equal,                 workspace, 12                                       # Рабочий стол №12
bind    = Super, TAB,                   workspace, m+1                                      # Следуюший рабочий стол
bind    = Super, Q,                     exec, $HotKey kill                                  # Убить окно
bind    = Super, W,                     killactive                                          # Закрыть окно
bind    = Super, E,                     exec, $HotKey rofi emoji                            # Открыть буфер эмодзи
bind    = Super, R,                     exec, $HotKey screenrecord                          # Быстрая запись экрана
bind    = Super, T,                     toggleFloating                                      # Плавающий режим
bind    = Super, I,                     exec, kitty -e ttyper                               # Слепая печать
bind    = Super, O,                     exec, kitty -e bash -lc "wthrr; exec bash"          # Погода
bind    = Super, P,                     exec, kitty -e dooit                                # Заметки
bind    = Super, bracketleft,           exec, kitty -e calcure                              # Календарь
bind    = Super, bracketright,          exec, dialect                                       # Переводчик
bind    = Super, backslash,             exec, gnome-calculator                              # Калькулятор
bind    = Super, A,                     exec, $HotKey panels sidebar                        # Открыть панель уведомлений
bind    = Super, S,                     exec, $HotKey screenshot                            # Создание быстрого скриншота
bind    = Super, D,                     exec, $HotKey window toggle                         # Скрыть окна на рабочем столе
bind    = Super, F,                     fullscreen                                          # Полноэкранный режим
bind    = Super, G,                     fullscreen, 1                                       # Нативный полноэкранный режим
bind    = Super, H,                     exec, $HotKey rofi keyhints                         # Подсказки сочетаний клавиш
bind    = Super, L,                     exec, $HotKey lock                                  # Заблокировать устройство
bind    = Super, C,                     exec, $HotKey rofi clipboard                        # Буфер обмена
bind    = Super, comma,                 workspace, m-1                                      # Предыдущий рабочий стол
bind    = Super, period,                workspace,  m+1                                     # Следуюший рабочий стол
bind    = Super, left,                  movefocus, l                                        # Фокус влево
bind    = Super, right,                 movefocus, r                                        # Фокус вправо
bind    = Super, up,                    movefocus, u                                        # Фокус наверх
bind    = Super, down,                  movefocus, d                                        # Фокус вниз




# =====================================================================================================
# ――――― Слой 2 ―― Super + Alt + {} ―― Запуск приложений ――――――――――――――――――――――――
# =====================================================================================================
bind  = Super Alt, Print,               exec, $HotKey screenshot ui                         # Система создания скриншота
bind  = Super Alt, W,                   exec, $HotKey rofi wallpapers                       # Выбор обоев рабочего стола
bind  = Super Alt, E,                   exec, thunderbird                                   # Почтовый клиент Thunderbird
bind  = Super Alt, R,                   exec, $HotKey screenrecord ui                       # Система записи экрана
bind  = Super Alt, T,                   exec, materialgram                                  # Социальная сеть Telegram
bind  = Super Alt, Y,                   exec, kitty -e yazi                                 # Файловый менеджер Yazi
bind  = Super Alt, B,                   exec, kitty -e yazi ~/Backups                       # Папка бекапов
bind  = Super Alt, I,                   exec, keypunch                                      # Тренировка слепой печати
bind  = Super Alt, O,                   exec, obsidian                                      # Записная книжка Obsidian
bind  = Super Alt, P,                   exec, seahorse                                      # Менеджер паролей
bind  = Super Alt, backslash,           exec, kitty -e btop                                 # Нагрузка на систему
bind  = Super Alt, S,                   exec, $HotKey screenshot ui                         # Система создания скриншота
bind  = Super Alt, D,                   exec, kitty -e calcure                              # ToDo заметки
bind  = Super Alt, F,                   exec, onlyoffice-desktopeditors                     # Офис
bind  = Super Alt, K,                   exec, kanri                                         # Kanban доска
bind  = Super Alt, semicolon,           exec, ptyxis                                        # Дополнительный терминал
bind  = Super Alt, apostrophe,          exec, kitty                                         # Дополнительный терминал
bind  = Super Alt, Return,              exec, ghostty                                       # Основной терминал
bind  = Super Alt, Z,                   exec, zeditor                                       # Редактор кода Zed
bind  = Super Alt, X,                   exec, devtoolbox                                    # Полезные утилиты для программиста
bind  = Super Alt, C,                   exec, $HotKey rofi snapcode                         # Сделать красивый скриншот кода
bind  = Super Alt, V,                   exec, code                                          # Редактор кода Vs Code
bind  = Super Alt, B,                   exec, flatpak run app.zen_browser.zen               # Браузер Zen
bind  = Super Alt, N,                   exec, kitty -e nvim                                 # NeoVim редактор
bind  = Super Alt, M,                   exec, musicpod                                      # Музыкальный плеер
bind  = Super Alt, period,              exec, flatpak run io.github.lainsce.Emulsion        # Цветовая палитра
bind  = Super Alt, slash,               exec, $HotKey hyprapps picker                       # Цветовая пипетка
binde = Super Alt, left,                exec, $HotKey window resize -50 0                   # Изменить размер окна влево
binde = Super Alt, right,               exec, $HotKey window resize 50 0                    # Изменить размер окна вправо
binde = Super Alt, up,                  exec, $HotKey window resize 0 -50                   # Изменить размер окна вверх
binde = Super Alt, down,                exec, $HotKey window resize 0 50                    # Изменить размер окна вниз




# =====================================================================================================
# ――――― Слой 3 ―― Super + Ctrl + {} ―― Изменение параметров ――――――――――――――――――――――
# =====================================================================================================
bind  = Super Ctrl, Escape,             exec, $HotKey power                                 # Меню питания устройства
bindt = Super Ctrl, 1,                  exec, $HotKey window move 1                         # Переместить окно на рабочий стол №1
bindt = Super Ctrl, 2,                  exec, $HotKey window move 2                         # Переместить окно на рабочий стол №2
bindt = Super Ctrl, 3,                  exec, $HotKey window move 3                         # Переместить окно на рабочий стол №3
bindt = Super Ctrl, 4,                  exec, $HotKey window move 4                         # Переместить окно на рабочий стол №4
bindt = Super Ctrl, 5,                  exec, $HotKey window move 5                         # Переместить окно на рабочий стол №5
bindt = Super Ctrl, 6,                  exec, $HotKey window move 6                         # Переместить окно на рабочий стол №6
bindt = Super Ctrl, 7,                  exec, $HotKey window move 7                         # Переместить окно на рабочий стол №7
bindt = Super Ctrl, 8,                  exec, $HotKey window move 8                         # Переместить окно на рабочий стол №8
bindt = Super Ctrl, 9,                  exec, $HotKey window move 9                         # Переместить окно на рабочий стол №9
bindt = Super Ctrl, 0,                  exec, $HotKey window move 10                        # Переместить окно на рабочий стол №10
bindt = Super Ctrl, minus,              exec, $HotKey window move 11                        # Переместить окно на рабочий стол №11
bindt = Super Ctrl, equal,              exec, $HotKey window move 12                        # Переместить окно на рабочий стол №12
bind  = Super Ctrl, P,                  pin                                                 # Зафиксировать окно
bind  = Super Ctrl, A,                  exec, $HotKey rofi animations                       # Выбор анимаций Hyprland
bind  = Super Ctrl, S,                  exec, $HotKey starship compact                      # Компактный режим starship
bind  = Super Ctrl, D,                  exec, $HotKey dnd toggle                            # Вкл/Выкл уведомления 
bind  = Super Ctrl, G,                  exec, $HotKey decorate game                         # Игровой режим
bind  = Super Ctrl, H,                  exec, $HotKey panels bar                            # Скрытие бара
bind  = Super Ctrl, J,                  exec, $HotKey panels bar toggle_move                # Отключение наведения у бара
bind  = Super Ctrl, O,                  exec, $HotKey decorate opacity                      # Изменить прозрачность окна
bind  = Super Ctrl, B,                  exec, $HotKey decorate blur                         # Изменить размытие фона
bind  = Super Ctrl, N,                  exec, $HotKey rofi nixos                            # Режим управления NixOS
bind  = Super Ctrl, M,                  exec, $HotKey decorate layout                       # Изменить макет окон
bind  = Super Ctrl, comma,              exec, $HotKey window move -                         # Переместить окно на предыдущий рабочий стол
bind  = Super Ctrl, period,             exec, $HotKey window move +                         # Переместить окно на следующий рабочий стол
bind  = Super Ctrl, left,               exec, $HotKey window shift l                        # Переместить окно влево
bind  = Super Ctrl, right,              exec, $HotKey window shift r                        # Переместить окно вправо
bind  = Super Ctrl, up,                 exec, $HotKey window shift u                        # Переместить окно вверх
bind  = Super Ctrl, down,               exec, $HotKey window shift d                        # Переместить окно вниз
bind  = Super Ctrl, slash,               exec, $HotKey clean ui                        # Переместить окно вниз



# =====================================================================================================
# ――――― Слой 4 ―― Super + Shift + {} ―― Media клавиши ―――――――――――――――――――――――――
# =====================================================================================================
bind = Super Shift, 1,                  exec, $HotKey shadow move 1                         # Перенести окно на скрытый рабочий стол 1
bind = Super Shift, 2,                  exec, $HotKey shadow move 2                         # Перенести окно на скрытый рабочий стол 2
bind = Super Shift, 3,                  exec, $HotKey shadow move 3                         # Перенести окно на скрытый рабочий стол 3
bind = Super Shift, 4,                  exec, $HotKey shadow move 4                         # Перенести окно на скрытый рабочий стол 4
bind = Super Shift, 5,                  exec, $HotKey shadow move 5                         # Перенести окно на скрытый рабочий стол 5
bind = Super Shift, T,                  exec, fragments                                     # Торрент клиент
bind = Super Shift, U,                  exec, $HotKey rofi links                            # Избраные URL адреса
bind = Super Shift, Z,                  exec, $HotKey player prev                           # Предыдующий трек
bind = Super Shift, X,                  exec, $HotKey player toggle                         # Play/Pause
bind = Super Shift, C,                  exec, $HotKey player next                           # Следуюший трек
bind = Super Shift, Space,              exec, $HotKey keyboard backlight up                 # Подсветка клавиатуры
bind = Super Shift, minus,              exec, $HotKey speaker down 5                        # Громкость -5
bind = Super Shift, equal,              exec, $HotKey speaker up 5                          # Громкость +5
bind = Super Shift, Backspace,          exec, $HotKey speaker toggle                        # Громкость вкл/выкл
bind = Super Shift, bracketleft,        exec, $HotKey brightness down 5                     # Яркость -5
bind = Super Shift, bracketright,       exec, $HotKey brightness up 5                       # Яркость +5
bind = Super Shift, backslash,          exec, $HotKey brightness cycle                      # Яркость цикл
bind = Super Shift, semicolon,          exec, $HotKey brightness down 2                     # Яркость -2
bind = Super Shift, apostrophe,         exec, $HotKey brightness up 2                       # Яркость +2
bind = Super Shift, comma,              exec, $HotKey speaker down 2                        # Громкость -2
bind = Super Shift, period,             exec, $HotKey speaker up 2                          # Громкость +2
bind = Super Shift, slash,              exec, $HotKey microphone toggle                     # Микрофон вкл/выкл
bind = Super Shift, W,                  exec, kitty -e yazi ~/Загрузки                      # Папка с загрузками
bind = Super Shift, G,                  exec, kitty -e yazi ~/Pictures                      # Папка с изображениями
bind = Super Shift, S,                  exec, kitty -e yazi ~/Pictures/Screenshots          # Папка со скриншотами
bind = Super Shift, R,                  exec, kitty -e yazi ~/Videos/Recordings             # Папка с записями экрана
bind = Super Shift, V,                  exec, kitty -e yazi ~/Videos                        # Папка с видео
bind = Super Shift, M,                  exec, kitty -e yazi ~/Music                         # Папка с музыкой
bind = Super Shift, B,                  exec, kitty -e yazi ~/Backups                       # Папка с бекапами
bind = Super Shift, J,                  exec, kitty -e yazi ~/Projects                      # Папка с проектами
bind = Super Shift, D,                  exec, kitty -e yazi ~/Documents                     # Папка с документами




# =====================================================================================================
# ――――― Слой 5 ―― Super + Alt + Shift + {} ―― Скрытое пространство ――――――――――――――――――
# =====================================================================================================
bind = Super Shift Alt, 1,              exec, $HotKey shadow space 1                        # Скрытый рабочий стол 1
bind = Super Shift Alt, 2,              exec, $HotKey shadow space 2                        # Скрытый рабочий стол 2
bind = Super Shift Alt, 3,              exec, $HotKey shadow space 3                        # Скрытый рабочий стол 3
bind = Super Shift Alt, 4,              exec, $HotKey shadow space 4                        # Скрытый рабочий стол 4
bind = Super Shift Alt, 5,              exec, $HotKey shadow space 5                        # Скрытый рабочий стол 5
bind = Super Shift Alt, Backspace,      exec, $HotKey shadow toggle                         # Режим тени
bind = Super Shift Alt, E,              exec, $HotKey shadow app "E" "thunderbird"          # Почтовый клиент  
bind = Super Shift Alt, T,              exec, $HotKey shadow app "M" "materialgram"         # Telegram
bind = Super Shift Alt, O,              exec, $HotKey shadow app "O" "obsidian"             # Obsidian
bind = Super Shift Alt, Y,              exec, $HotKey shadow app "Y" "kitty -e yazi"        # Файловый менеджер
bind = Super Shift Alt, P,              exec, $HotKey shadow app "P" "seahorse"             # Менеджер паролей
bind = Super Shift Alt, backslash,      exec, $HotKey shadow app "S" "kitty -e btop"        # Нагрузка на систему
bind = Super Shift Alt, S,              exec, $HotKey shadow app "P" "secrets"              # Менеджер секретов
bind = Super Shift Alt, D,              exec, $HotKey shadow app "D" "kitty -e calcure"     # ToDo заметки
bind = Super Shift Alt, K,              exec, $HotKey shadow app "K" "kanri"                # KanBan доска
bind = Super Shift Alt, semicolon,      exec, $HotKey shadow app "T2" "ptyxis"              # Дополнительный терминал
bind = Super Shift Alt, apostrophe,     exec, $HotKey shadow app "T" "kitty"                # Дополнительный терминал
bind = Super Shift Alt, Return,         exec, $HotKey shadow app "W" "ghostty"              # Терминал
bind = Super Shift Alt, Z,              exec, $HotKey shadow app "Z" "zeditor"              # Редактор кода Zed
bind = Super Shift Alt, X,              exec, $HotKey shadow app "X" "devtoolbox"           # Утилиты для программиста
bind = Super Shift Alt, V,              exec, $HotKey shadow app "V" "code"                 # Редактор кода VsCode
bind = Super Shift Alt, B,              exec, $HotKey shadow app "B" "flatpak run app.zen_browser.zen --private-window"  # Браузер Zen в режиме инкогнито
bind = Super Shift Alt, N,              exec, $HotKey shadow app "N" "kitty -e nvim"        # Редактор кода NeoVim
bind = Super Shift Alt, M,              exec, $HotKey shadow app "M" "musicpod"             # Музыкальный плеер
bind = Super Shift Alt, period,         exec, $HotKey shadow app "C" "flatpak run io.github.lainsce.Emulsion" # Цветовая палитра
